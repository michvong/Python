[pytest]
# Don't treat repo utility scripts as test/doctest targets
norecursedirs = scripts .* build dist .venv venv __pycache__ docs

markers = mat_ops: matrix operation tests

# If doctest collection is enabled, limit it to real packages/dirs (optional)
# You can still run doctests explicitly later if needed.
addopts = -q
